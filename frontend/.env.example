# Assidu — variables d'environnement du frontend.
# Copier ce fichier en .env puis ajuster.

# URL du backend Spring Boot (SANS le suffixe /api).
# Les appels /backend-api/* sont réécrits vers ${NEXT_PUBLIC_API_BASE_URL}/api/*.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
